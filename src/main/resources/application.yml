spring:
  jpa:
    show-sql: true
  application:
    name: java-advanced
  profiles:
    active: ${PROFILE:local}

jwt:
  secret: senha-super-secreta
  issuer: wellwork-gs
  expiration: 3600

security:
  enabled: true
